import { useAffirmationStore } from '../../stores/affirmationStore';
import { dayOfYearSeed } from '../../utils/date';
export const AffirmationBoard = () => { const { affirmations } = useAffirmationStore(); const prioritized = [...affirmations].sort((a, b) => Number(b.favorite) - Number(a.favorite)); const idx = prioritized.length ? dayOfYearSeed(new Date()) % prioritized.length : -1; const daily = idx >= 0 ? prioritized[idx] : undefined; return <section className="rounded-2xl border border-white/10 bg-slate-900/70 p-5"><h2 className="text-xl font-bold text-white">힘이 되는 말 보드</h2><div className="mt-3 rounded-xl bg-gradient-to-r from-violet-500/20 to-cyan-500/20 p-4"><p className="text-sm text-slate-300">오늘의 문구</p><p className="text-lg text-white">{daily?.content ?? '아직 문구가 없습니다.'}</p></div></section>; };
