import { useState } from 'react';
interface Props { open: boolean; apiKey: string; modelName: string; onSave: (key: string, model: string) => void; onDelete: () => void; }
export const ApiKeyModal = ({ open, apiKey, modelName, onSave, onDelete }: Props) => { const [key, setKey] = useState(apiKey); const [model, setModel] = useState(modelName); const [show, setShow] = useState(false); if (!open) return null; return <div><input type={show?'text':'password'} value={key} onChange={(e)=>setKey(e.target.value)}/><button onClick={()=>setShow(!show)}>👁</button><input value={model} onChange={(e)=>setModel(e.target.value)}/><button onClick={()=>onSave(key,model)}>저장</button><button onClick={onDelete}>키 삭제</button><p>API 키는 이 기기의 브라우저에만 저장됩니다. 공용 PC에서는 사용 후 반드시 키를 삭제해주세요.</p></div>; };
