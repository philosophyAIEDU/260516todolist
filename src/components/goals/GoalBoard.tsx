import { useGoalStore } from '../../stores/goalStore';
export const GoalBoard = () => { const { goals } = useGoalStore(); const done = goals.filter((g) => g.achieved).length; const rate = goals.length ? Math.round((done / goals.length) * 100) : 0; return <section className="rounded-2xl border border-white/10 bg-slate-900/70 p-5"><h2 className="text-xl font-bold text-white">나의 목표 비전보드</h2><p className="mt-2 text-slate-300">{goals.length}개 중 {done}개 달성 ({rate}%)</p><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-700"><div className="h-full bg-emerald-400" style={{ width: `${rate}%` }} /></div></section>; };
